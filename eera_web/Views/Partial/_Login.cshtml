@model eera_model.User
<div style="color:red">@ViewBag.errorMessage</div>
<div id="modal1" class="modal fade" role="dialog">
    <div class="log-in-pop">
        <div class="log-in-pop-left">
            <h1>Hello...</h1>
            <p>Don't have an account? Create your account. It's take less then a minutes</p>
            @*<h4>Login with social media</h4>
            <ul>
                <li>
                    <a href="#"><i class="fa fa-facebook"></i> Facebook</a>
                </li>
                <li>
                    <a href="#"><i class="fa fa-google"></i> Google+</a>
                </li>
                <li>
                    <a href="#"><i class="fa fa-twitter"></i> Twitter</a>
                </li>
            </ul>*@
        </div>
        <div class="log-in-pop-right">
            <a href="#" class="pop-close" data-dismiss="modal">
                <img src="~/images/cancel.png" alt="" />
            </a>
            <h4>Login</h4>
            <p>Don't have an account? Create your account. It's take less then a minutes</p>
                @using (Html.BeginForm("UserLogin", "Home", FormMethod.Post)){ 
                <div>
                    <div class="input-field s12">
                        @*@Html.TextBox("txtMailId", null,new { @class = "validate", data_ng_model="name"})*@
                        @Html.TextBoxFor(m => m.MailID, new { @class = "validate", data_ng_model = "name" })
                        <label id="lblUsename">Email Id</label>
                    </div>
                </div>
                <div>
                    <div class="input-field s12">
                        @*@Html.TextBox("txtPassword", null, new { @class = "validate", @data_ng_model = "name", @type = "password" })*@
                        @Html.TextBoxFor(m => m.Password, new { @class = "validate", @data_ng_model = "name", @type = "password" })
                        <label id="lblPassword">Password</label>
                    </div>
                </div>
                <div>
                    <div class="s12 log-ch-bx">
                        <p>
                            <input type="checkbox" id="test5" />
                            <label for="test5">Remember me</label>
                        </p>
                    </div>
                </div>
                <div>
                    <div class="input-field s4">
                        <input type="submit" id="bntLogin" value="Login" class="waves-effect waves-light log-in-btn">
                    </div>
                </div>
                }
                <div>
                    <div class="input-field s12"> <a href="#" data-dismiss="modal" data-toggle="modal" data-target="#modal3">Forgot password</a> | <a href="#" data-dismiss="modal" data-toggle="modal" data-target="#modal2">Create a new account</a> </div>
                </div>            
        </div>
    </div>
</div>

